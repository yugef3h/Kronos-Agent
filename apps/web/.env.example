# 前端 Vite 环境变量（复制为 apps/web/.env.local）

# API 基址（可选）
# - 不设置：pnpm dev 时走 Vite proxy（/api → localhost:3001），请求同源无 CORS
# - 设置后：直连该地址，跳过 proxy（如连远程后端）
# VITE_API_BASE_URL=http://localhost:3001

# Vite dev proxy 目标（仅 vite.config 读取，可选）
# VITE_API_PROXY_TARGET=http://localhost:3001
