import React from "react"

const StartPanel = () => {
  return (
    <div className="w-full h-full flex items-center justify-center">
      <div className="text-gray-500">Start Node</div>
    </div>
  )
}

export default React.memo(StartPanel)