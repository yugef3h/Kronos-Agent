import React from "react"

const IfElsePanel = () => {
  return (
    <div className="w-full h-full flex items-center justify-center">
      <div className="text-gray-500">If-Else Node</div>
    </div>
  )
}

export default React.memo(IfElsePanel)