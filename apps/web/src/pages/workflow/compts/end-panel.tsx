import React from "react"

const EndPanel = () => {
  return (
    <div className="w-full h-full flex items-center justify-center">
      <div className="text-gray-500">End Node</div>
    </div>
  )
}

export default React.memo(EndPanel)