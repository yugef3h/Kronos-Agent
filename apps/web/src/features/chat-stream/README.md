# Chat Stream Feature

- Core runtime panel for SSE chat, uploads, takeout branching, and history restore.
- Public surface stays small: import `ChatStreamPanel` from `features/chat-stream`.
- Markdown rendering remains delegated to `components/MarkdownMessage`.